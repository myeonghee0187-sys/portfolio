import './Hero.css'

/** 자리만 잡아둔 placeholder. 실제 Hero 디자인은 다음 작업에서 채웁니다. */
export default function Hero() {
  return (
    <section className="hero" id="hero">
      <h1 className="hero__title">PORTFOLIO</h1>
      <p className="hero__note">Hero placeholder</p>
    </section>
  )
}
